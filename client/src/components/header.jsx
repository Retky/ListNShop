import './header.scss';

const header = () => {
  const header = (
    <header>
      <nav />
      <div className="logo">
        <h1>list N shop</h1>
      </div>
      <div className="userBtn" />
    </header>
  );

  return header;
};

export default header;
